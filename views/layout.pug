doctype html
html
  head
    block head
      title="Busy Bees - Home"
      link(rel='stylesheet' href='/stylesheets/layout.css')

  body
    div(id="top-nav")
      div(class="top-container")
        div(id="logo") LOGO
        div(id="buttons")
          a(type="button" class="demo-btn btn" href="/users/login") Demo User
          a(type="button" class="whats-new-btn btn" href="") What's New
          a(type="button" class="login-btn btn" href="/users/login") Log in
          a(type="button" class="signup-top-btn btn" href="/users/sign-up") Sign up for free
    block content
